// import capitalizeFn from "../function/capitalizeFn.js"
// import randomFn from "../function/randomFn.js"


// const complex = (state) => {
// 	let name = capitalizeFn(randomFn(state.words.iYouHeShe.concat(state.words.nounsName)))
// 	let adjectives = randomFn(state.words.adjectives)
// 	let ing = randomFn(state.words.verbVThree.map(el => el[3]))
// 	let nouns = randomFn(state.words.nouns)
// 	let vThre = randomFn(state.words.verbVThree.map(el => el[2]))

// 	let phraseOne = name + ' ' + adjectives
// 	let phraseTwo = name + ' ' + ing
// 	let phraseThre = name + ' ' + nouns
// 	let phraseFour = name + ' ' + vThre

// 	let phrase = randomFn([phraseOne, phraseTwo, phraseThre, phraseFour])

// 	return phrase
// }

// export default complex