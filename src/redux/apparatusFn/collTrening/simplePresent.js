// import capitalizeFn from "../function/capitalizeFn.js"
// import randomFn from "../function/randomFn.js"


// const simplePresent = (state) => {
// 	let name = capitalizeFn(randomFn(state.words.iYouHeShe.concat(state.words.nounsName)))
// 	let verb = randomFn(state.words.verbVThree.map(el => el[0]))
// 	let modal = randomFn(state.words.modalVerb)
// 	let verbFirstPosition = randomFn(state.words.verbFirstPosition.map(el => el[0]))
// 	let phraseOne = name + ' ' + verb
// 	let phraseTwo = name + ' ' + modal + ' ' + verb
// 	let phraseThre = name + ' ' + verbFirstPosition + ' ' + verb
// 	let phrase = randomFn([phraseOne, phraseTwo, phraseThre])

// 	return phrase
// }

// export default simplePresent